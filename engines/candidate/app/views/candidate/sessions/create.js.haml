- if @session.errors.any?
  $("#modal").html("#{escape_javascript(render 'error')}");
  $("#modal").modal("show");
- else
  $("#modal").html("#{escape_javascript(render 'success')}");
  $("#modal").modal("show");
  setTimeout(function() { window.location.href = "#{candidate.root_path}"; }, 3000)

update_js();